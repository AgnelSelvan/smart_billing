import 'package:flutter/material.dart';

class AppColors {
  static Color primaryColor = const Color(0xFFFFBB0D);
  static ColorScheme primaryColorScheme =
      ColorScheme.fromSeed(seedColor: AppColors.primaryColor);
}
